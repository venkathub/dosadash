# CLAUDE.md — DosaDash Project Instructions

## Project

DosaDash: AI-native South Indian cloud kitchen platform. Portfolio project for AI Engineer role, production-deployed on AIC Cloud VPS (4 GB RAM). Read `docs/` for the full plan before making architectural decisions. `docs/05-schedule-12-weeks.md` defines the current phase — always work within the current phase's scope.

## Tech Stack (fixed — do not substitute without asking)

- **Backend**: Python 3.12, FastAPI, Pydantic v2, SQLAlchemy 2.0 async, Alembic, Celery + Redis
- **DB**: PostgreSQL 16 + pgvector (vectors + FTS in same DB — deliberate; no separate vector DB)
- **AI**: LangGraph (agents), litellm (routing: gpt-4o-mini primary → Groq gpt-oss-120b → Gemini Flash fallback; Groq retired Llama 3.3 on 2026-08-16), OpenAI text-embedding-3-small, Groq whisper-large-v3 (STT)
- **Bot**: aiogram (Telegram), webhook mode (not polling) in production
- **Frontend**: Next.js (App Router) + Tailwind — one app, three surfaces: customer `/`, KDS `/kds`, admin `/admin`
- **ML**: XGBoost (forecasting, ETA), implicit ALS (recommender), LoRA fine-tune (sentiment), MLflow registry
- **Ops**: Docker Compose (single VPS), Caddy, GitHub Actions CI/CD via SSH, Langfuse Cloud, promptfoo/pytest evals

## Repository Layout

```
apps/api    → Core FastAPI (auth, menu, orders, payments, admin)
apps/ai     → AI service (agents, RAG, guardrails, MCP server, ML inference)
apps/bot    → aiogram Telegram adapter (thin — NO business logic here)
apps/web    → Next.js (customer + KDS + admin)
packages/ml → datagen, forecasting, recsys, fine-tuning
packages/shared → Pydantic schemas shared across services
evals/      → golden datasets, suites, LLM-as-judge rubrics
knowledge/  → RAG source markdown (recipes, allergens, FAQs, policies)
infra/      → docker-compose.yml, Caddyfile, deploy scripts
```

## Hard Rules

1. **Provider interfaces**: payments (`PaymentProvider`), OTP delivery (`OtpChannel`), LLM calls (litellm only — never call OpenAI SDK directly). Swappability is a core story of this project.
2. **No hallucinated menu items**: every `item_id` the order agent emits MUST be validated against the DB before order placement. This guardrail is non-negotiable.
3. **Structured outputs everywhere**: agent outputs are Pydantic models (`OrderDraft` etc.), never free-text parsing.
4. **Event cascade**: mutations to business state (menu edit, kitchen pause, 86'd item) publish to Redis pub/sub; AI layer subscribes (re-embed RAG, bust caches, agent behavior). Never let the AI layer drift from business state.
5. **Evals are merge gates**: changes to prompts/agents/RAG must pass `evals/` suites in CI. Add eval cases for every new agent capability.
6. **Trace everything**: every LLM call goes through litellm with Langfuse callback (session_id, user_id, prompt version tag).
7. **4 GB RAM budget**: no local LLMs/Whisper on the VPS. All inference via APIs. Check the memory budget in `docs/02-architecture.md` before adding services.
8. **PII**: redact phone numbers before LLM calls and logs.
9. **Secrets**: env vars only (`pydantic-settings`), never committed. Razorpay = TEST keys only.
10. **Telegram bot is an adapter**: it normalizes I/O and renders inline keyboards; all reasoning lives in `apps/ai`.

## Conventions

- Python: ruff (lint+format), pytest, type hints mandatory, async-first
- API: REST under `/api/v1`, WebSockets at `/ws/*`, health at `/healthz`
- Order states: `PLACED → CONFIRMED → COOKING → READY → OUT_FOR_DELIVERY → DELIVERED` (+ `CANCELLED`, `REFUNDED`) — transitions only via `order_service` state machine
- Roles: `customer | kitchen_staff | admin | owner` (JWT claims, RBAC per route)
- Commits: conventional commits (`feat:`, `fix:`, `chore:`, `evals:`)
- Prompts: versioned files in repo (`apps/ai/prompts/`), tagged in Langfuse

## Domain Notes

- Menu: South Indian — dosa varieties, idli/vada, pongal, biryani, Chettinad curries, filter coffee. Veg/vegan/Jain flags, spice levels, allergens matter.
- Synthetic data: festival multipliers (Pongal ×3 idli/pongal, Diwali sweets), weekend biryani spikes — see `packages/ml/datagen`.
- Eval languages: English, Hinglish, Tanglish conversations in golden sets.
- Currency INR, GST on bills (5% food), prices realistic (dosa ₹80–180).

## Git Workflow (docs/08 — mandatory)

- `main` = production, protected. **Never commit directly to main.**
- Each schedule phase = its own `phase/N-*` branch; small `feat/*`/`fix/*` PRs merge into the phase branch; completed phase squash-merges into `main` via a Phase PR using the checklist in docs/08.
- Merge to `main` = production deploy. Rollback = revert PR.
- Conventional commits. PRs touching `apps/ai/**`, prompts, or `evals/**` must update eval cases.

## Current Status

- [x] Planning complete (docs/)
- [x] Phase 0: Foundation — COMPLETE (merged to main #2, deployed via CI)
- [x] Phase 1: Core platform + Auth — COMPLETE (PRs #3–#11: menu APIs, OTP/JWT/RBAC, order state machine + checkout, KDS + WS + event bus, customer web UI, Razorpay TEST, addresses/preferences, Telegram linking + DM OTP + unlink)
- [x] Phase 2: Admin Backend I — COMPLETE (PRs #13–#19 into phase branch: menu ops, settings/pause/staff-RBAC/audit, order mgmt + refunds, combos/ingredients/recipes, nutrition LLM enrichment [first litellm + Langfuse + eval-gate code], hours/schedule enforcement, admin web UI; migrations c41f7a2d9b03 + e7b9c4d15a22; squash-merged to main, deployed via CI)
- [x] Phase 3: RAG + Order Agent — COMPLETE (PRs #21–#28 into phase branch: knowledge base + generated allergen guide, hybrid RAG + citations + re-embed cascade, LangGraph order agent + DB-validated guardrail, SSE streaming + prompt caching, web + Telegram adapters on one graph; migrations a3f8d21c7b90 + b7c4e92f1a05; live evals 18/18 · 12/12 · 15/15 @ 0.95 gate, Langfuse traces verified; squash-merged to main via Phase PR, deployed via CI)
- [x] Phase 4: Evals + LLMOps — COMPLETE (PRs #31–#37 into phase branch, squash-merged to main via Phase PR, deployed via CI: golden set 18→150 tagged conversations + coverage-floor asset gates; suites order_accuracy / tool_correctness / guardrail_bypass / tone [first LLM-as-judge rubric, tone_v1] + retrieval + RAG faithfulness; one-pass run_live_evals.py; **CI live merge gate live-evals.yml @ order_accuracy ≥ 0.95** — caught real regressions at 85% and 92.67% pre-merge, fixed via data calibration + prompts order_agent_v2/v3 [name fidelity, cross-lingual numerals, removal/replace semantics, allergen-checked suggestions]; final 150-case run **96.67% / tool 100% / 0 bypasses**; eval_runs table [migration f8c2d94a1b37] + CI ingest [EVAL_INGEST_URL + INTERNAL_API_TOKEN secrets set] + admin Evals scoreboard tab; semantic cache [Redis semcache:*, cosine ≥ 0.95, cascade-flushed on menu events + re-ingest, Q&A only]; cost dashboard [Langfuse daily metrics → ai /internal/costs → api RBAC proxy → admin Costs tab])
- [x] Phase 5: Classical ML + Admin II — COMPLETE (`phase/5-ml-reports`, PRs #40–#46; Phase PR #47 squash-merged to main, deployed via CI, post-deploy smoke verified in prod): schema [a9e4b71c3d58: forecasts, customer_segments, orders.delivered_at + state-machine stamping; datagen delivery durations] · Celery worker+beat [dedicated `redis-celery` noeviction broker (cache redis is allkeys-lru — unsafe); IST beat: 02:00 forecast, 03:00 CRM, hourly heartbeat; compose limits ≈2.4 GB] · XGBoost demand forecast [MLflow registry + champion alias, promote iff WAPE improves; **v1 WAPE 0.421 vs naive lag-7 0.555**; artifacts committed → baked into images; nightly recursive 14-day upsert — first prod run: 616 rows / 44 dishes] · ETA regression [ai-service /internal/eta at checkout + heuristic fallback; **MAE 3.35m at synthetic noise floor (oracle 3.32m), P90 6.84 vs 7.00**; prod smoke: 38 min @ dosadash-eta/v1] · CRM segments [quintile RFM, personal-rhythm churn `1−exp(−recency/2·gap)`, LTV — first prod run: 509 users / 7 tiers] · Reports + admin tabs [sales IST rollups, dish P&L w/ labeled 35% cost fallback, gst.csv, forecast-vs-actual SVG + day/dish anomaly flags, CRM win-back ltv×churn] · **Text-to-SQL copilot** [copilot_v1, SQL guardrail (SELECT-only/allowlist/phone-ban/LIMIT≤200), READ ONLY txn + 4s timeout, `dosadash_readonly` role b3f9c82d4e61 (prod-verified: DELETE denied), 2-round self-correction, admin tab w/ charts; eval-gated: copilot_guardrail.jsonl 27 cases, zero false accepts/rejects + allowlist-coherence gates; prod smoke: valid SQL first attempt via gpt-4o-mini, PII probe refused with `SELECT 1 AS unsupported`]
- [x] Phase 6: Agents + MCP — COMPLETE (feat PRs #49–#55 → `phase/6-agents-mcp`, Phase PR #56 squash-merged to main, deployed via CI + infra hotfix #57, full post-deploy smoke verified in prod): **Slice 1: inventory agent + wastage log + owner approval** — schema [migration c9d4e82f7a13: suppliers (backfilled from free-text `ingredients.supplier`, kept for display), `ingredients.supplier_id`, purchase_orders + items w/ agent provenance, wastage_log w/ stock_after snapshots; new `pubsub:inventory` channel — deliberately OFF pubsub:menu so stock changes never re-embed RAG] · wastage endpoints [kitchen_staff can log; atomic decrement, clamp-at-0 w/ audited `clamped` flag] · inventory agent [apps/ai: deterministic needs math (Σ forecast × recipe qty vs stock+reorder buffer) → LLM pass (PODraftBatch, prompt inventory_agent_v1) → guardrail re-anchors to needs table: hallucinated ingredients dropped, qty clamped [deficit, 3×deficit], omissions force-added, supplier grouping canonical; deterministic fallback when LLM chain fails; /internal/inventory/draft-po] · nightly beat inventory.nightly_po @ 02:30 IST (post-forecast) w/ open-PO dedup per supplier · po_service state machine [DRAFT→PENDING_APPROVAL→APPROVED→RECEIVED (+REJECTED/CANCELLED); receive increments stock] · admin PO endpoints [list/detail/draft-now/line-edit w/ cost recompute/approve/reject/receive/cancel, all audited] + admin web Inventory tab [PO cards, qty editor, run-agent-now, wastage quick-log] · Telegram owner approval [api→bot /internal/po-notify card; po:approve/reject callbacks → /api/v1/internal/po/decision, RBAC re-checked api-side; api config bot_base_url] · evals: inventory_po.jsonl (14 cases, 7 adversarial) + test_inventory_assets.py key-free CI gates (zero hallucinations, band clamps, coverage, prompt-constant coherence) · 37 new tests, full suite 382 passed. **Slice 2: supplier invoice OCR** — first VLM path [litellm data-URL image parts, vision-only chain gpt-4o-mini → Gemini (Groq text model skipped); InvoiceExtraction structured output, prompt invoice_extract_v1 w/ no-guessing rule; deterministic arithmetic verifier (earned confidence, model never asserts its own); invoice↔PO matcher (seq + token containment names, ±10% qty, flags short deliveries/padded bills/missing lines); invoices table (migration d7f3a91c5e24) + review queue (combined confidence ≥ 0.8 → MATCHED pre-checked, else PENDING_REVIEW; human approve ALWAYS required → PO RECEIVED → stock in); admin upload/approve/reject + Inventory-tab invoice section; evals invoice_match.jsonl 12 cases + gates incl. no-discrepancy-clears-the-gate] · **Slice 3: support agent** — [fresh DB order context, SupportTurn structured output, guardrail: foreign/hallucinated order_ids downgraded, LLM-failure → escalation; api executes: status (ownership re-checked), cancel via state machine (PLACED-only, refusal → escalation), refunds NEVER agent-executed → escalations table (migration e8a5d47b9c31); admin inbox resolve(+real provider refund)/dismiss; customer 🛟 chat on orders page + admin Support tab; evals support_guardrail.jsonl 10 cases + no-foreign-id-executes invariant] · **Slice 4: long-term memory** — [user_memories (migration f2c6e83a1d47), EPISODE per placed order (atomic, CASCADE w/ user); context loader derives "my usual" (most-repeated signature 90d ≥2, CANCELLED excluded) + last 3 episodes → volatile STATE (cache-stable prefix untouched); prompt **order_agent_v4** rule 14 (honour usual EXACTLY, never invent); golden set 150→154 w/ memory tag+floor, harness seed_usual_orders — **prompt bump must pass the CI live gate**] · **Slice 5: MCP server** — [`dosadash-mcp` stdio console script (mcp SDK): get_menu/check_inventory/place_order as thin HTTP adapter; api /internal/mcp/* endpoints run the REAL order_service as 'Claude (MCP demo)' customer; docs/09-mcp-demo.md Claude Desktop config + demo script; zero VPS RAM (runs client-side); handshake smoke-tested] · full suite 426 passed · also fixed IST-window flake in forecast-anomaly test · **Phase PR gate: 154 cases order_accuracy 96.75% / tool 100% / 0 bypasses** (first run 94.81% — 4 of 8 fails were documented flakies, clean on re-run; separate CI-side rate-limit fail on #55 after 4 back-to-back runs, also clean on re-run) · **infra hotfix #57**: worker needed API_INTERNAL_API_TOKEN (+ai/bot base URLs) — first prod nightly_po hit 403, caught in post-deploy smoke · **prod smoke (2026-08-19 IST)**: migrations at f2c6e83a1d47; inventory agent PO #1 (42 needs, gpt-4o-mini, dedup on re-run) → approve → receive → stock 0→40kg, audited; wastage 40→38; MCP place → order #40632 ₹189 + hallucinated-item 404; episode memory row → **"my usual" drafted exactly on order_agent_v4**; live support agent get_status + social-engineering probe declined; refund chat → escalation #1 → inbox resolve; live VLM invoice extraction (all fields + arithmetic correct, PENDING_REVIEW w/o matching PO, rejected); Langfuse callback active (all paths traced); seeded prod supplier 'Koyambedu Wholesale Supplies' linked to all 42 ingredients
- [x] Phase 7: Voice, Vision, Recsys, Promos — COMPLETE (`phase/7-multimodal-recsys`, feat PRs #60–#70; Phase PR #71 squash-merged to main 8330cc7, deployed via CI + hotfix #72; **full post-deploy smoke 15/15 PASS in prod**; feat PRs #60–#65 merged into phase branch via merge commits, all CI gates green incl. live evals; merged-tree verification: 516 tests + 70 eval-suite + web build): **Slice 1 voice ordering** [first STT path: litellm groq/whisper-large-v3, no fallback chain (only STT provider; failure → "please type"); transcript phone-redacted before leaving ai (audio unredactable — invoice-VLM trade-off); api /chat/telegram/stt proxy; bot F.voice: 90s cap, mime normalize, echo 🎤 → SAME order-agent graph via run_agent_turn refactor; EN+Tamil auto-detect; golden 154→158 voice tag+floor — schedule-gated dishes (Sweet/Ven Pongal) must stay out of eval expectations] · **Slice 2 ALS recommender** [implicit ALS, log1p confidences (raw counts LOST to popularity 0.363 vs 0.378 — caught by sweep), f=64/α=5/reg=0.5: **Recall@10 0.417 vs pop 0.378, MAP 0.220 vs 0.211, tail-Recall@10 0.345 vs structural 0.000** (the personalization story on a 52-item catalog); MLflow dosadash-recsys champion, promote iff recall improves; ITEM factors only exported — serve folds LIVE DB history via ALS normal equations (synthetic user ids never leak; predict.py numpy-only, implicit/scipy stay in ml[train]); ai /internal/recs degrades als→embedding-cold-start (cart-centroid cosine, in-process cache flushed by menu cascade)→DB popularity; 86'd items never recommended; api GET /recs never fails the menu page (unavailable→empty); web 'You might like' strip; asset gates: champion must beat popularity + tail>0.2; retrain: `-m dosadash_ml.recsys.train --synthetic` + commit artifacts] · **Slice 3 checkout combo suggester** [pure rules in dosadash_ml.recsys.suggest shared verbatim between serving and measurement: combo-completion (APPROVED combo missing exactly 1 cart item) outranks pairing gaps (Beverages/Sweets/Snacks — score picks the CATEGORY too: first cut's fixed priority LOST to random 11.8% vs 12.8%, caught by the A/B sim); absim 3 arms × 3,273 holdout checkouts, persona-ground-truth acceptance: **attach 15.6% vs random 12.8%, AOV +4.92% vs control** (caveat in artifact: validates taste recovery, not real-world uplift); CI gates personalized>random + uplift>0; ai /internal/recs/checkout on shared ranking pass; web footer chips 🧩/✨] · **Slice 4 dish-photo QC** [third VLM path; model OBSERVES only (dish_qc_v1: is_food_photo/dishes_seen/presentation_issues/confidence, 'NEVER list a dish you cannot actually see'), verdict computed deterministically: chain-fail→UNREADABLE, non-food/blurry→CHECK (never PASS even if dishes claimed — invariant-gated), missing dish→MISMATCH (outranks issues), issues→CHECK, else PASS; matching w/ pack-size strip + singularization ('two idlis'↔'Idli (2 pcs)' — plural bug caught by eval gate pre-live); api POST /orders/{id}/qc-photo STAFF_ROLES, expected dishes from real order rows; KDS 📷 button (capture=environment) + verdict badge; stateless, no migration; dish_qc.jsonl 13 exact-match cases] · **Slice 5 coupon engine** [migration b5e8d24f7a91: coupons +is_active (born INACTIVE)/min_subtotal/max_discount/per_user_limit/source(MANUAL|AI_SUGGESTED)/description, orders.discount; validation ladder w/ human messages (active→window→min-subtotal-with-₹-gap→global→per-user); GST on DISCOUNTED amount pro-rata; usage=counting redemption rows; preview advisory, checkout re-resolves (CouponError→400 no order); admin guardrails PCT≤50%+mandatory cap, FLAT≤₹300+min_subtotal≥2× (no free food), redeemed→deactivate-only; web checkout coupon input + admin Coupons tab] · **Slice 6 promo agent** [inventory-agent pattern for promos: deterministic MINING (90d co-ordered pairs ≥3 orders, combo-covered pairs excluded; slow weekday IST, median AOV, existing codes) → LLM copy only (promo_agent_v1, PromoDraftBatch) → guardrail (hallucinated pairings dropped, price clamped [85%,97%]×parts, omissions force-added, PCT 5–30 w/ cap defaulted from median AOV, FLAT 20–150 w/ 2× floor, codes normalized+deduped) → deterministic fallback, never 5xx; api /admin/promos/suggest RE-VALIDATES everything (same validate_coupon_values humans face) → combos AI_SUGGESTED/DRAFT + coupons AI_SUGGESTED/inactive into EXISTING approval flows, per-item skip reasons, audited; 🤖 button on Coupons tab; promo_guardrail.jsonl 10 cases + invariants: sanitized output must pass api admin validation, free food impossible under adversarial batch, prompt-band coherence] · **Slice 7 menu localization I — Tamil translation store + LLM drafter + owner approval** (PR #66, feat/l10n-schema-translator) [migration c6e2f84a9d17: menu_item_translations PK(item_id,lang), DRAFT/APPROVED/REJECTED + model/prompt_version/reviewed_by provenance — prices/allergens/flags NEVER stored, canonical row stays SoT; nutrition-enrichment pattern: ai /internal/translate/menu w/ prompt menu_translation_v1, chunked structured_completion (8/call), deterministic guardrail (hallucinated item_ids dropped, omissions reported never fabricated, names must be in target script w/ numerals surviving verbatim — invented digits/₹ refused; bad description/label discarded not fatal; dead chunk ≠ dead batch, all-dead → 502); api /admin/translations draft (gap-fill default — draft-all never resets human-reviewed rows; explicit ids = deliberate re-draft→DRAFT) / list / edit (resets DRAFT) / approve-reject, audited + menu.translation event; admin web Translations tab; shared translations.py language registry (add a lang = name+script-range+SUPPORTED, coherence-gated); evals translation_guardrail.jsonl 14 cases w/ adversarial coverage floor + key-free gates (numeral invariant, script check, prompt↔schema coherence); 15 new tests — suite 531 passed, eval suites 77 passed; NOT serving to customers yet — surfaces (?lang=ta / web toggle / bot pref / agent context) are slice 8, Tamil golden set + live-gate floor slice 9] · **Slice 8 menu localization II — serve Tamil across api/web/agent** (PR #67, feat/l10n-surfaces) [api ?lang=ta on GET /menu + /categories + /items/{id}: APPROVED-only per-field overlay (name/description/category_label) w/ canonical fallback, category stays canonical key + canonical_name exposed for search, unsupported lang 422; **staleness safe-by-default**: admin PATCH touching name/description/category resets that item's APPROVED translations→DRAFT in the SAME transaction (audited translations_reset) — stale Tamil never serves, price/spice edits don't reset; **agent aliases**: approved translated names join menu context as `aliases` (guardrail still resolves item_ids, canonical name untouched → name-fidelity + "my usual" unaffected; order_agent_v4 unchanged, no prompt bump) — key emitted ONLY when aliases exist so menu context is BYTE-IDENTICAL to pre-l10n when none approved (prefix caching + live gate zero drift, invariant-gated in translation asset suite); web EN/தமிழ் toggle (localStorage) + localized headings + bilingual search; bot deliberately unchanged (agent auto-detects, prefs.language already in context since Phase 1); 10 new tests — suite 541, eval suites 79; live gate passed clean first try] · **Slice 9 menu localization III — Tamil golden set + per-language gate floor** (PR #68, evals/tamil-golden-set) [golden 158→168: 10 Tamil-script cases ta-001..010 (basic/multi-item/Tamil confirm/edit/remove/86'd-via-alias/hallucination/Tamil injection/code-switched/piece-count qty-null), expectations on CANONICAL names; harness setup.seed_translations seeds approved rows PER CASE + restore deletes (non-Tamil cases keep byte-identical context, 0 rows left after run); run_live_evals per-language metrics as flat lang_accuracy_<code> floats (ingest contract dict[str,float]) + LANGUAGE_GATES ta ≥ 0.80 (TA_ACCURACY_GATE overridable; 10 cases → 1 wobble = 10 pts, floor absorbs documented flakies, broken language still fails hard); asset gates: lang "ta" + ≥10 floor + ta-cases-must-seed-aliases + seeds real-item/supported-lang/target-script; pre-push local validation 10/10 live; **CI gate on merged set: 168 cases, order_accuracy 96.43%, tool 100%, 0 bypasses, lang_accuracy ta 1.00 / en 0.969 / hinglish 0.967 / tanglish 0.935**; order_agent_v4 untouched — Tamil rides the #67 alias mechanism] · **Slice 10 AI menu photos** (PR #69, feat/menu-image-gen) [migration d8f1a63c9e25: menu_image_drafts (PK item_id, full-prompt provenance) + menu_items.image_ai; ai /internal/imagegen/menu-item via litellm aimage_generation — single provider **gpt-image-1** quality=low (~₹1/image), STT precedent no fallback; versioned style contract menu_image_v1 (45° food photography, NO text/watermarks/people, veg never shows meat), prices never reach the model; **pre-merge live smoke caught two real API drifts mocks couldn't: dall-e-3 retired upstream + response_format param removed** — verified end-to-end 1024×1024 PNG via production path; api /admin/menu-images generate (PNG-magic validated, media/menu files, re-generate never clobbers the live file)/list/approve (sets image_url + image_ai=true — customers ALWAYS see the ✨ AI label)/reject (deletes file, unpublishes if live), audited + menu.image event; /media StaticFiles + API_MEDIA_DIR + Caddy /media/* + compose apimedia named volume; customer cards render photo w/ AI badge, admin Images tab; test_imagegen_assets.py 6 key-free gates (style contract/prompt round-trip/no-price/schema); 10 tests] · **Slice 11 mock-aggregator channel** (PR #70, feat/mock-aggregator; docs/04 O12) [migration e5b9d72f4a83: aggregator_orders (aggregator, external_order_id) UNIQUE → idempotent webhook retries (duplicate=true); POST /api/v1/aggregator/webhook HMAC-SHA256 raw-body X-Aggregator-Signature (Razorpay pattern, 503 unconfigured/403 bad sig) → SAME order_service.create_order, channel=MOCK_AGGREGATOR (Phase 0 enum finally cashed), name-resolved items (unknown → 422 nothing persisted), 86'd → 409, pause/hours enforced; prepaid via AggregatorPrepaidProvider behind the PaymentProvider ABC (CAPTURED at ingest, refunds work); customer upsert labeled "(via mockswiggy)"; signed GET status poll (path-identity signature); admin /simulate (RBAC+audited) + 🛵 button on Orders tab; KDS channel badges 🛵/✈️ (payload always carried channel, now rendered); compose API_AGGREGATOR_WEBHOOK_SECRET (api only — generate on-VPS); 9 tests — suite 560 passed] · **ALL docs/05 week-10 deliverables complete — nothing was cut** · **DEPLOYED (2026-08-20)**: Phase PR #71 squash → main → CI deploy; **first deploy crash-looped the api** (fresh root-owned apimedia volume → EACCES on mkdir at import → 502 smoke) — recovered in prod within minutes (volume chowned to appuser/1000, api healthy), then **hotfix #72** (Dockerfile bakes /app/media ownership for fresh volumes + main.py media mount degrades on OSError instead of dying — media is a nice-to-have, checkout never dies for it), hotfix deploy green re-proving the pipeline · pre-deploy: AGGREGATOR_WEBHOOK_SECRET generated into infra/.env on-VPS · **post-deploy smoke (in prod, 15/15 PASS)**: l10n draft→approve→/menu?lang=ta "மசாலா தோசை" w/ canonical_name · anonymous agent chat "ஒரு மசாலா தோசை" drafts Masala Dosa via the alias · gpt-image-1 photo generated→published→menu shows image_ai=true→/media serves 1.38MB PNG (left live — demo value) · aggregator webhook order #40634 PLACED→replay duplicate=true→signed status poll 200→bad sig 403→admin simulate #40635 · recs 6 items · admin coupons 200 · migrations at e5b9d72f4a83, all containers healthy)
- [x] Phase 8: Fine-tune + Reviews — COMPLETE (`phase/8-finetune-reviews` off main d00cce3, feat PRs #74–#78 all CI + live gate green; **Phase PR #79 squash-merged to main 815fe7b, deployed via CI (2m49s, healthz smoke green), full prod smoke PASS on 2026-08-20**): **Slice 1 reviews platform** (PR #74, feat/reviews-schema-datagen) [migration b6f4a92c7d18: reviews table (docs/06 shape) — UNIQUE order_id, NULL sentiment = unscored, scoring provenance (scored_model/prompt_version/at) + reply flow (reply_draft backoffice-only vs published owner_reply w/ reply_source AI_DRAFT|MANUAL); shared REVIEW_ASPECTS registry (8 aspects — single source of truth for datagen labels, LLM guardrail, fine-tune label space, coherence-gated); datagen reviews.py: deterministic template-built reviews w/ PLANTED (aspect,polarity) labels — en/hinglish/tanglish only (Latin script by design → DistilBERT-friendly), persona aspect bias, trailing-21d "oily dosa" complaint spike (trend-alert demo signal), ~1% planted PII ("call me back on +91…") to exercise Rule 8 end-to-end, ~22% of orders reviewed, ~8% rating-only; seed.py inserts rating/text ONLY (planted labels NEVER land in DB — scoring models are evaluated against held-out datagen labels, not leaked ones); customer POST/GET /orders/{id}/review (DELIVERED-only, one per order, foreign order = 404 not 403); 21 tests; seeder smoke 60d/80users → 1095 orders / 213 reviews / 0 scored] · **Slice 2 zero-shot scoring + inbox** (PR #75, feat/reviews-zero-shot-inbox) [ai /internal/reviews/score (prompt review_sentiment_v1): chunked (10/call) batch tagging; phones redacted BEFORE the LLM; rating-only reviews scored from stars w/ ZERO LLM calls (provenance `deterministic:rating`); guardrail drops hallucinated review_ids + off-registry aspects (incl. injection-obeying output), dedupes, and RECOMPUTES review-level sentiment from kept aspects (dish-QC philosophy: model observes, verdict computed); dead chunk ≠ dead batch, all-dead → 502 · ai /internal/reviews/draft-reply (review_reply_v1): guardrail — compensation promises (refund/discount/free/coupon/"on the house"; "feel free to" trips it too, over-block to safety), contact data, overlength → deterministic sentiment-matched template fallback instead, NEVER 5xx; fallbacks must pass their own guardrail (gated) · api admin inbox: list w/ filters + dish context / score-pending w/ per-row provenance / draft-reply / publish (reply_source=AI_DRAFT ONLY when published verbatim — one edited char → MANUAL; publish-twice 409) / trends (weekly NEGATIVE mention counts per aspect, alert = latest ≥3 AND ≥2× trailing mean, top offending dishes), all audited · web admin Reviews tab (trend bars + ⚠ spike alerts, aspect chips ▲▼, draft→edit→publish) + customer ★ rate-order box on delivered orders · evals: review_sentiment.jsonl 15 cases + review_reply_guardrail.jsonl 12 cases + key-free gates (no off-registry aspect ever survives, planted-PII never reaches LLM messages, prompt↔registry↔datagen coherence); 38 tests] · **Slice 3 LoRA fine-tune + benchmark** (PR #76, feat/lora-sentiment-train) [packages/ml/finetune: dataset (16-label multi-hot = registry × polarity; sha256 hash-split 80/10/10 keyed on ORDER INDEX so template edits can never migrate examples across the train/test boundary — gated), metrics.py = ONE shared label-set scorer for BOTH benchmark sides (no torch), train CLI (DistilBERT + LoRA q/v r=8 + trained head, multi-label BCE, CPU by design ~10min/epoch, MLflow dosadash-sentiment champion iff macro-F1 improves), benchmark CLI · torch/transformers/peft in a uv dependency GROUP (not an extra) + CPU-only wheel index — CI --all-extras and VPS images never pull torch; local: `uv sync --group finetune` · live zero-shot side evals/suites/review_zero_shot_eval.py runs the PRODUCTION prompt+guardrail path on the shared test split (250 sampled), cost from litellm token counts at published prices · **committed artifacts (benchmark.json): LoRA v2 macro-F1 0.9944 vs gpt-4o-mini zero-shot 0.9926 on the SAME 250-review holdout (LoRA wins by +0.18pts), exact 0.988 vs 0.980, ₹0 vs ₹3.20 per 1k reviews, 34.9ms/review CPU; full test (772) macro-F1 0.9956; adapter 2.9MB committed** (honesty note in artifact: planted-label recovery, not human agreement) · key-free gates test_finetune_assets.py: macro floor 0.95, gap ≤3pts, economics recorded, adapter-sized-commit guard, label-space drift, predictions never leave registry; 22 tests] · **Slice 4 INT8 ONNX CPU serving** (PR #77, feat/sentiment-onnx-serving) [export CLI export_onnx.py: merge LoRA → ONNX (classic exporter, opset 17) → dynamic INT8; **QUInt8 chosen EMPIRICALLY over QInt8 (macro-F1 0.9937 vs 0.9875 against fp32 0.9956 — recsys-log1p lesson again: measure, don't assume)**; parity measured THROUGH the exact serving code path and recorded in the committed artifact meta (67.4MB, size-gated at 90MB) · torch-free serving dosadash_ml.finetune.predict via new `dosadash-ml[sentiment]` extra (onnxruntime + tokenizers + numpy; torch stays in the finetune group — VPS images never pull it); intra_op threads capped at 2 · **/internal/reviews/score is local-first**: INT8 champion (~57ms/review, ₹0) scores text reviews; deterministic confidence contract (no prob in threshold±0.15 band, ≥1 label fired, no contradictory both-polarity aspect) — **confident coverage 97.2% @ macro-F1 0.9968**; unconfident → LLM chain; missing/corrupt artifact → degrade to LLM (hotfix-#72 pattern), force_llm escape hatch · provenance: scored_model `local:dosadash-sentiment/v2-int8`, scored_prompt_version NULL; response local_ids/local_model; ReviewScoreRunOut extended · key-free gates test_sentiment_serving_assets.py: LIVE recompute on 200-review holdout through the committed binary, quantization gap ≤2pts, confidence-contract-earns-its-keep, coverage floor 0.7, size guard, label-space drift, margin↔constant coherence · docs/02 + compose budget updated (ai 512M→768M, total ≈2.6GB; Rule 7 wording clarified: no local LLM/Whisper — tiny classical/tuned models serve locally by design); 6 new tests + 7 gates] · **Slice 5 nightly Batch API scoring** (PR #78, feat/reviews-nightly-batch) [/score defer_llm mode: local + rating-only score now, unconfident residue returned as deferred_ids (deferring is success, never 502) · ai batch-submit: phone-redacted chunked JSONL (**Rule 8 applies to batch FILES — gated**) via litellm files/batches, **OpenAI only, no fallback (STT/image-gen single-provider precedent)** — failure leaves reviews for the next run; batch-poll ingests through the SAME sanitize_scores guardrail (every review scored or rejected, never lost — gated invariant) · review_batch_jobs table (migration c8e5a97d3f62, up+down verified): durable job state + authoritative custom_id→review_ids chunks mapping (ai stays stateless) + SUBMITTED rows = in-flight dedup; batch provenance lives here — litellm's Langfuse callback doesn't fire for files/batches endpoints (honest note in code) · Celery beat: reviews.nightly_scoring 03:30 IST (after CRM) + reviews.batch_poll hourly @ :20; local scores commit BEFORE submit (provider failure never rolls back done work); batch results NEVER clobber scores landed in between (admin on-demand wins) · services/review_scoring.py: ONE provenance mapping for all three writers (admin on-demand / nightly / poller) — `deterministic:rating` / `local:<version>` (no prompt) / live model / `batch:<model>` · no compose changes (worker already has ai URL+token via hotfix #57, keys stay ai-side); 14 new tests + 3 gates] · **suite 641 + eval suites 115; live gate green on all five feat PRs AND the Phase PR (168 golden cases — order_agent_v4 untouched all phase)** · **pre-merge staging**: ai image built from the phase tree, INT8 champion smoked IN-CONTAINER under a 768M cgroup (202MB peak RSS, 61ms/review — postmortem-#71 lesson: image-level assumptions verified before deploy) · **prod smoke (2026-08-20 IST, all PASS)**: migrations at c8e5a97d3f62, all containers healthy, ai 316MB/768MB w/ champion loaded · champion serves in prod (delivery+temperature NEGATIVE on the late-cold probe) · seeded 5 reviews on delivered orders → admin score-pending: **2 scored via local:dosadash-sentiment/v2-int8 w/ NULL prompt version + 1 deterministic:rating, model=None — ZERO LLM calls** · manual reviews.nightly_scoring: oily-vada review w/ planted phone number scored LOCALLY (**PII never left the box**), slang review deferred → **real Batch API job batch_6a8713…ad3af SUBMITTED (chunks [[4]], gpt-4o-mini)** · batch_poll in_progress passthrough ×2 → **COMPLETED + ingested ~5min later: batch:gpt-4o-mini, taste:NEGATIVE, scored=1/failed=0** · reply flow: gpt-4o-mini draft (guardrail-clean, dish context correct) → published verbatim → reply_source=AI_DRAFT · trends counting delivery/freshness/temperature negatives, no false alerts · only ai-log noise: benign litellm logging-worker 'Output file id is None' during the two in_progress polls (its own cost-prefetch, not our path) · **smoke addendum (same day, closing the last DoD gaps in prod)**: customer review path exercised end-to-end (POST /orders/40617/review 201 → duplicate 409 → GET hides backoffice fields) · **unconfident→live-LLM escalation proven in prod** (slang review: local 0, scored_model gpt-4o-mini, prompt review_sentiment_v1) · **Langfuse traces confirmed via API** for both live review paths (sessions reviews:score + reviews:reply, tagged review_sentiment_v1 / review_reply_v1 — Rule 6) · **scheduled beat observed firing autonomously** (review-batch-poll at 21:20 IST slot, clean empty-queue run); nightly-review-scoring fires tonight 03:30 IST — logic already proven via manual trigger
- Notes for continuation: eval flakies to watch (ord-093/099/109/138/146, now also ord-040/060/074/095 seen once — wobble, not regressions; if a gate fails with only these, re-run before debugging; back-to-back live runs can also exhaust OpenAI/Groq rate limits → gate fails on fallback-to-Gemini-without-key, just re-run spaced out) · tone judge is opt-in (`run_live_evals.py --with-tone`), not in the CI gate · GEMINI_API_KEY secret optional/unset (chain: OpenAI → Groq) · retrain/promote: `uv run python -m dosadash_ml.forecasting.train --synthetic` / `-m dosadash_ml.eta.train --synthetic`, then commit `packages/ml/artifacts/` (mlflow.db/mlruns gitignored) · `READONLY_DB_PASSWORD` live in infra/.env (generated on-VPS, never in repo/transcript) · worker logs: `docker compose logs worker` (hourly heartbeat; nightly beats 02:00 forecast / 02:30 inventory PO / 03:00 CRM IST — first runs triggered manually in prod) · owner Telegram PO cards need the owner account (user 510) to link Telegram — currently unlinked, `notified=0` is expected until then · MCP demo: `docs/09-mcp-demo.md` (needs `INTERNAL_API_TOKEN` from infra/.env; demo customer +919000000099) · compose is in `infra/` on the VPS (`cd /opt/dosadash/infra`); api image has no curl — smoke with `docker compose exec api python` + httpx · **Phase 7 state (2026-08-20)**: work continues on `phase/7-multimodal-recsys` — feat PRs #60–#65 all merged into the phase branch via a local merge-commit train (#62/#65 show CLOSED not MERGED — stacked PRs whose content landed via their parents); **PRs #66–#70 ALL MERGED, Phase PR #71 squash-merged to main (8330cc7) and DEPLOYED via CI on 2026-08-20 + hotfix #72 (media-volume permissions postmortem)** — **Phase 7 COMPLETE, nothing cut, post-deploy smoke 15/15 PASS in prod**; feat branches deleted · **prod state**: migrations at e5b9d72f4a83 · AGGREGATOR_WEBHOOK_SECRET live in infra/.env (generated on-VPS, never in repo/transcript) · apimedia volume owned by appuser (uid 1000) — hotfix #72 bakes ownership into the image for FRESH volumes, but if the volume is ever recreated manually verify ownership before blaming the app · Masala Dosa has a LIVE approved Tamil translation + a LIVE published AI photo (left in place for demo value) · aggregator smoke orders #40634/#40635 exist w/ channel MOCK_AGGREGATOR · postmortem lesson worth repeating: **the deploy pipeline's 502 was caught by its own /healthz smoke — and the fix pattern is now standard (nice-to-have mounts degrade, never crash the app)** · **Phase 8 state (2026-08-20)**: COMPLETE — all five week-11 slices via PRs #74–#78, **Phase PR #79 squash-merged to main (815fe7b) and DEPLOYED via CI, prod smoke all-PASS (nothing cut)**; feat branches deleted; new prompts review_sentiment_v1 + review_reply_v1 (order agent still order_agent_v4); LoRA retrain: `uv sync --group finetune` then `uv run --group finetune python -m dosadash_ml.finetune.train --synthetic --epochs 5`, zero-shot side `evals/suites/review_zero_shot_eval.py` (keys from .env.eval), then `-m dosadash_ml.finetune.benchmark`, then **re-export serving artifact `-m dosadash_ml.finetune.export_onnx`** and commit `packages/ml/artifacts/sentiment/` (incl. onnx/ — 67MB INT8 binary is deliberate, size-gated); sentiment serving lives in `dosadash_ml.finetune.predict` (onnxruntime via `dosadash-ml[sentiment]` extra, ai image only); batch scoring: worker beats reviews.nightly_scoring 03:30 IST + reviews.batch_poll hourly @ :20 (jobs table `review_batch_jobs`, provenance `batch:<model>`; OpenAI-only, ai-side keys) · **prod state additions**: migrations at c8e5a97d3f62; 5 smoke reviews live on orders 40618–40620/40630/40631 (one w/ published AI_DRAFT owner reply); review_batch_jobs #1 COMPLETED (real OpenAI batch round-trip); local docker daemon can't see /data/aiTrack (snap/VM confinement) — build staging images via tar-pipe: `tar -czf - . | docker build -f apps/ai/Dockerfile -t x -`; admin smoke auth pattern: mint owner JWT in-container via create_access_token (user 510) · benign-noise note: litellm logging-worker logs 'Output file id is None' tracebacks on every in_progress batch poll — its own cost-prefetch, not our path, ignore · **ALL 12 WEEKS / 10 PHASES COMPLETE (2026-08-20)** — remaining work is human-only: record the demo video (docs/10 script), publish the blog (docs/11 draft), link both from the README · new prod state after Phase 9: demo accounts +919000000011 (kitchen_staff) / +919000000012 (admin) seeded (re-run `seed --demo-accounts` if a demo admin demotes them); bot memory limit 192M (hotfix #90 — measure RSS with a 320M test container before ever lowering it); rate limiting live (tune via `API_RATE_LIMIT_*` env); cache counters live in `cachestats:*` on the cache redis (prod prompt-cache share 48.9% on first measurement; semcache counters only move via /internal/rag/answer — customer chat rides the agent path which deliberately bypasses it); smoke Q&A cache entries for 'ghee roast vegan'/'peanut dishes' left in redis (TTL 24h) · keep future slices as small feat/* PRs into a phase branch · new since Phase 6: prompts dish_qc_v1 + promo_agent_v1 + menu_translation_v1 + menu_image_v1 (order agent still order_agent_v4); golden set now 168 (voice tag+floor; Tamil "ta" language + ≥10 floor + per-language live-gate floor ta ≥ 0.8); new key-free eval gates dish_qc/promo_guardrail/translation_guardrail/imagegen/recsys-champion/ab-sim; root `pytest` testpaths EXCLUDE evals/ — run `uv run pytest evals -q` separately (CI has a dedicated Eval suites job) · local full suite needs pg on :5433: `docker run -d --rm --name dosadash-test-pg -e POSTGRES_USER=dosadash -e POSTGRES_PASSWORD=dosadash -e POSTGRES_DB=dosadash -p 5433:5432 pgvector/pgvector:pg16` (tests skip without it) · recsys retrain: `OPENBLAS_NUM_THREADS=1 uv run python -m dosadash_ml.recsys.train --synthetic` then re-run `-m dosadash_ml.recsys.absim` and commit `packages/ml/artifacts/recsys/` (CI gates: champion beats popularity, tail>0.2, personalized>random attach, uplift>0) · live-gate flake addendum: PR #61 failed the gate once with ZERO prompt/golden changes while identical code passed on #62 — clean on re-run (`gh run rerun <id> --failed`); treat no-prompt-change gate failures as flaky-first · merge-train conflict hotspots for future stacked work: dosadash_shared/ai.py + __init__.py (sections append at the same anchor), ai main.py router block, api ai_client.py method tail, web page.tsx checkout footer — after any merge resolution run a full-tree `git grep '<<<<<<<'` AND import both apps (a stray marker slipped through once and was only caught two merges later)
- [x] Phase 9: Hardening + Story — COMPLETE (`phase/9-hardening`, feat PRs #82–#88; **Phase PR #89 squash-merged to main aaed124 and DEPLOYED via CI (2026-08-20) + hotfix #90; full prod smoke PASS same evening**): **Slice 1 rate limiting** (PR #82): pure-ASGI middleware (SSE-safe, deliberately not BaseHTTPMiddleware), fixed-60s-window Redis counters on the cache redis; tiers by cost exposure chat 20/min · auth 10/min · write 60/min · read 240/min (env-tunable `API_RATE_LIMIT_*`); identity = JWT user id else first X-Forwarded-For hop; exempt healthz/media/internal/docs/WS + valid X-Internal-Token (bot funnels all Telegram users through one IP); fail-open on redis outage; 429 + Retry-After + X-RateLimit-* headers; 27 tests; general-suite conftest disables the limiter (dedicated tests install a MemoryStore) · **Slice 2 cache observability** (PR #83): semcache hit/miss/store/flush counters in `cachestats:semcache` (OUTSIDE the semcache:rag:* prefix — cascade flush never wipes stats, gated); llm client accumulates `usage.prompt_tokens_details.cached_tokens` per completion (`cachestats:prompt`); ai GET /internal/costs/cache → api /admin/costs/cache (RBAC) → Costs-tab cache panel; best-effort everywhere; agent byte-identical; threshold/TTL tuning deferred until prod numbers (measure-first) · **Slice 3 locust** (PR #84): infra/loadtest/ scenarios (anonymous browse incl. Tamil, demo-OTP customers, env-gated orders/chat), locust in a dependency GROUP; **local baseline: 100 users / 2564 reqs / 0 failures / agg P50 24ms P95 210ms, checkout P95 370ms over 91 real orders; limiter shed 343 reqs @ served P50 17ms; read-tier probe exactly 240×200 then 429** · **Slice 4 demo page** (PR #85): web `/demo` + `seed --demo-accounts` (+919000000011 kitchen_staff, +919000000012 admin; OWNER deliberately has no public credential — test-gated invariant) · **Slice 5 story I** (PR #86): README rewritten (mermaid arch, measured-metrics table w/ provenance + caveats inline, live URL) + docs/07 resume bullets real-numbers-only (unverifiable aspirational claims dropped) · **Slice 6 story II** (PR #87): docs/10 shot-by-shot 3-min video script + docs/11 complete publishable blog draft ('Evals Are Merge Gates') — recording/publishing = human tasks, writing done · **PR #88 test-card fix** (user-reported): 4111… is NOT a Razorpay test card (fails at checkout) — replaced with official Visa 4386 2894 0766 0153 / MC 2305 3242 5784 8228 + mock-bank Success/Failure + OTP-length rule, per razorpay.com/docs (their docs warn third-party card lists go stale) · **Phase PR #89 live gate: 95.24% / tool 100% / 0 bypasses / ta 1.00** (first run 94.64% — ALL fails from the documented flaky set, clean on spaced re-run; scoreboard runs #35 fail → #36 pass) · **hotfix #90 (postmortem, #57/#72 pattern)**: post-deploy smoke found the bot OOM-crash-looping (exit 137) — the Phase-9 uv.lock refresh grew the rebuilt bot image's steady-state RSS to ~151 MiB > the long-standing 128M limit (NO bot code changed all phase); measured in prod w/ a 320M test container BEFORE raising → limit 192M, docs/02 budget updated (bot now ~126 MiB stable, total ≈2.4 GB) · **prod smoke (2026-08-20 ≈23:30 IST, all PASS)**: main at aaed124+hotfix, all containers healthy · `seed --demo-accounts` run · /demo serves w/ corrected cards via Caddy · demo-admin OTP login end-to-end (channel DEMO, role admin) · /admin/costs/cache live · **prompt-cache measured in prod: 48.9% cached-token share over 2 live agent calls** (the stable-prefix strategy proven with provider numbers) · **semcache proven in prod via /internal/rag/answer: miss→store→exact hit, hit_rate 0.33, Hinglish paraphrase honestly below 0.95 threshold** (note: customer chat rides the AGENT path which deliberately bypasses semcache — prod counters only move via the rag/answer surface) · limiter live: auth tier exactly 10×200 then 429+Retry-After, X-RateLimit headers on organic reads · **prod off-peak locust (limiter ON, orders OFF): 454 reqs / 0 failures / agg P50 41ms P95 140ms through TLS+Cloudflare, 3 auth 429s by design** — recorded in results.md + README · REMAINING (human): record video (docs/10), publish blog (docs/11)
- [x] Phase 10 (post-schedule): UI "Heritage Luxe" premium redesign — COMPLETE (`phase/10-ui-premium`, Phase PR #93 squash-merged to main 6659749, deployed via CI 2026-08-21, prod smoke PASS + hotfix #94): spec approved in docs/12-ui-premium-design.md (banana-leaf green #14342B + brass #C8A24B + cream, Fraunces/Inter/Noto Sans Tamil kolam identity) · token layer [literal-hex Tailwind theme + CSS vars; **lesson: Tailwind opacity modifiers (bg-x/95) silently no-op with bare `var(--x)` colors — caught by Playwright visual pass pre-merge**] · self-hosted variable woff2 fonts committed (~310 KB, NO build-time Google fetch — deterministic deploys; Tamil finally renders in a real font) · `app/components/ui.tsx` primitives + one `statusBadgeTone()` (deleted the 7 drifted per-file btnCls/STATUS_COLORS copies) · all 5 routes restyled (customer menu/dock/Dosa-Genie-chat/tracker, /orders, KDS accent-bar board 44px targets no-backdrop-blur, admin grouped rail Operations·Growth·AI Studio·System w/ .ai-meta provenance chips, /demo) · visual-only — zero API/WS/SSE/localStorage changes except Razorpay overlay `#f59e0b`→`#14342B`; live-eval gate correctly not triggered (no ai/evals paths); agent menu context untouched · **hotfix #94 (postmortem, #57/#72/#90 pattern): prod /media/* 404 discovered during post-deploy smoke — NOT caused by Phase 10: Caddyfile is a single-file bind mount, `git reset --hard` in deploy replaces the host inode, the mount keeps exposing the OLD inode, so the deploy's `caddy reload` had been faithfully re-reading pre-Phase-7 config on every deploy (caddy container up since 08-16; Phase 7's /media smoke tested the api in-container, never the public path); fixed live via `docker compose restart caddy`, then deploy.yml unconditionally restarts caddy after `up` (hotfix #95 — the first fix #94 checksum-probed via `compose exec`, which exits 1 under the CI SSH session with no diagnostics though it works over plain ssh; deploys recreate api/web and drop all WS connections anyway, so an unconditional ~1s caddy restart adds zero extra disruption)** · prod smoke: Heritage Luxe live on all surfaces, மசாலா தோசை + AI photo + ✨ badge + Tamil toggle verified public
- [x] Phase 11 (post-schedule): Highway menu rebuild + serving windows — CODE COMPLETE on `phase/11-highway-menu` (stacked PRs #96–#99 merged via local merge-commit train; **Phase PR to main + deploy + prod migration/smoke still pending**): **catalog** researched from two real Chennai–Trichy NH-45 landmarks — 99 KM Coffee (Perumbairkandigai, pure-veg millet kitchen) → Millet Specials (Ragi Millet Dosa, Kambu Idli, Thinai Pongal, Kuzhi Paniyaram) + Mini Tiffin + Sukku Coffee; Manna Mess (Acharapakkam, non-veg mess) → Mess Specials (Mutton Chukka, Nattu Kozhi Kuzhambu, Karuvadu Thokku, Nandu Pepper Fry, Prawn Thokku) + lunch-only Non-Veg Mess Meals; 52→60 dishes (4 unreferenced retired via `RETIRED_DISH_NAMES`, 86'd not deleted), new fish/shellfish allergen groups, personas updated · **serving windows on EVERY dish** ("Dosa is not available in Lunch" enforced end-to-end): multi-window schedule shape `{day: [{start,end}…]}` (tiffin 06:00–11:30 + 17:00–22:00; curries/biryani/rice 11:30–22:00; veg-meals two sittings; mess meals lunch-only; pongal mornings; sweets/beverages all day), `serving_windows_text()` humanizer, `DOSADASH_NOW_IST` clock pin (availability.now_ist only, unset in prod) · **migration f4a7b62c8d19** (up→down→up verified; fresh-DB-safe via EXISTS(brands) guard — caught when CI-style seed crashed; backfills windows by name, upserts new dishes+recipes idempotently) · **surfaces**: GET /menu annotates off-window dishes (available_now + serving_windows) instead of hiding; checkout 409 says WHEN; web greyed cards + ⏰ badge + cart guard; admin schedule display multi-window-safe · **agent (the hard-won lesson, 15 live-gate runs)**: EVERY design that exposed serving-hours to gpt-4o-mini — available:false flags, a not_serving_now list, timing text in retrieval chunks, meal_periods/good_for fields, even a "meals at lunch" aside in the MENU description — made it hallucinate refusals of orderable dishes (85–93%); final design = dish-QC philosophy: **menu payload lists ORDERABLE dishes only (presence = orderability, meal_periods no longer serialized), `guardrail.serving_notes` appends the deterministic window/sold-out story to the reply (alias-aware), `guardrail.drop_substitutions` blocks 86'd-sibling swaps (Mysore-for-Masala), prompt order_agent_v5** (resolve-before-refusing, qty-binds-to-following-dish, zero serving vocabulary); timing knowledge centralized in knowledge/serving-hours.md (per-dish serving lines REMOVED from guides — they poisoned retrieval); "Ragi Dosa" renamed "Ragi Millet Dosa" (one edit from Rava Dosa → agent substituted) · **evals**: golden 168→174 (serving_window tag+floor 4, tim-001..006), pinned-clock gate rework (required dishes must be on-schedule at EVAL_CLOCK_IST 19:30; serving_window cases must forbid an off-window dish), retired 'sevai' from ord-146 answers · **final local live gate: 174 cases — order_accuracy 95.40%, tool 100%, 0 bypasses, ta 1.00 / en 0.970 / hinglish 0.968 — ALL PASS** · full suite 695 + eval suites 115 + web build green · recsys retrained w/ new `--force-promote` (catalog changes make incumbent recall incomparable): v4 Recall@10 0.387 vs pop 0.352, tail 0.304; ab-sim attach 15.7% vs 13.3% · **flaky-set update**: vce-002 (curd-rice refusal), vce-003 (Tanglish qty swap), tim-002, ord-020/022/023/028/035/041/084/088/096/110/111/117 seen wobbling across runs at temp 0 — the new-menu wobble pool; re-run before debugging · **deploy checklist for the Phase PR**: after prod migration, trigger re-embed for the 12 new dishes (menu.created never fired — admin PATCH each or re-ingest), draft+approve Tamil translations for new dishes, optionally generate AI photos, verify /menu?lang=ta and the ⏰ badge at lunch IST
- Update this checklist as phases complete.
