# Copy to infra/.env on the VPS (and locally). Never commit real values.

ENV=prod

# Set to your domain for automatic HTTPS via Caddy; leave :80 for plain HTTP
DOMAIN=:80

POSTGRES_USER=dosadash
POSTGRES_PASSWORD=change-me
POSTGRES_DB=dosadash

# --- Added in later phases (placeholders, keep empty until needed) ---
# OPENAI_API_KEY=
# GROQ_API_KEY=
# GEMINI_API_KEY=
# LANGFUSE_PUBLIC_KEY=
# LANGFUSE_SECRET_KEY=
# TELEGRAM_BOT_TOKEN=
# RAZORPAY_KEY_ID=        # TEST keys only
# RAZORPAY_KEY_SECRET=    # TEST keys only
